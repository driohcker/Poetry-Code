# 贡献指南

感谢你对 **码诗 · CodePoetry** 的兴趣！

## 如何贡献

1. **Fork 本仓库**
2. **创建你的诗作文件**，放置在正确的目录下
3. **更新 `metadata.json`**，在对应作者目录下添加作品信息
4. **提交 Pull Request**，并在描述中说明：「这句诗为什么用这行代码表达」

## 目录规范

```
src/<朝代拼音>/<作者全拼>/<作品英文名>.<后缀>
```

## 命名规范

| 层级       | 规则                                                          | 示例                        |
| :------- | :---------------------------------------------------------- | :------------------------ |
| **朝代目录** | 拼音小写，`pre_qin` / `tang` / `song` / `yuan` / `ming` / `qing` | `src/tang/`               |
| **作者目录** | 全拼小写，无空格，姓在前                                                | `libai/`、`liqingzhao/`    |
| **文件命名** | 英文意译，小写，下划线连接                                               | `quiet_night_thoughts.js` |
| **元数据**  | 每个作者目录下固定 `metadata.json`                                   | `metadata.json`           |

## 提交前检查清单

- [ ] 代码可运行（通过对应语言的解释器/编译器）
- [ ] 除注释与日志外，正文中无中文字符
- [ ] 已更新作者目录下的 `metadata.json`
- [ ] PR 描述中说明了诗与代码的映射理由
